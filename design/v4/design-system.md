# Glance Design System v1.10

> **Status**: Stable / 可施工
> **Supersedes**: design/v3/tokens-v1.md(全部数值);v1.0 的修订见文末 Changelog
> **图纸**: `design/v4/Glance 面板 v1.10.html`(自包含;`template.html` + `build.py` 可再生;产物一版一名并自动清理旧版,防 file:// 缓存冒名顶替与开错历史文件)
> **交互语义**: 沿用 `design/brand-spec.md` 冻结项,本文不重复;本文只管**视觉与动效工艺**

本文关键词 **必须(MUST) / 禁止(MUST NOT) / 应当(SHOULD)** 按 RFC 2119 语义使用。数值偏离本文即视为缺陷,不提 PR、直接报 bug。

---

## 0. 不变量(Invariants)

以下任何版本迭代**禁止**违反:

1. 双容器、不嵌套;浮窗水平中心**必须**正对选中 App 图标,超界收进语境屏。
2. 全系统彩色仅两源:聚焦蓝 `--accent`、红绿灯三色。**禁止**引入第三种彩色。
3. **禁止**振荡/弹簧物理;**禁止**任何动画超过 240ms。唯一许可的欠阻尼:选中卡浮起(单次 settle,overshoot ≤1.35,无二次振荡,v1.1 评审拍板)。240ms 上限管关键帧动画与状态过渡;材质光(高光质心、色散)的连续跟随与演示布景不受此限(v1.4 修订)。
4. 图标**必须**以原始 squircle alpha 渲染,**禁止** CSS 裁形;图标阴影**必须**用贴合轮廓的方式(drop-shadow / 图片层阴影),**禁止**矩形 box-shadow。
5. 窗口卡**禁止**出现标题栏带以外的文字、按钮、角标。
6. `prefers-reduced-motion` 下所有动效**必须**归零。

## 1. 度量系统

**一切尺寸是 4 的倍数。** 唯一例外:0.5px 发丝线与红绿灯(10px,系统规格)。

### 间距阶梯

| 阶梯 | 值 | 用途 |
|---|---|---|
| s1 | 4 | 微调,仅用于图标选中上浮等亚阶梯补偿 |
| s2 | 8 | 图标格间距、红绿灯间距 |
| s3 | 12 | 标题栏左缘等局部留白 |
| s4 | 16 | 长条/浮窗内边距、卡间距、浮窗↔长条间距 |
| s5+ | 24/32 | 页面级留白,面板内不出现 |

规则:同层元素间距**必须**取自同一阶梯值(卡与卡、卡与边同为 s4——一个容器内只允许一阶节奏)。

### 圆角阶梯(同心规则:外角 = 内角 + 间距,取整到阶梯)

| 阶梯 | 值 | 用途 |
|---|---|---|
| r2 | 10 | 窗口卡(= macOS 窗口圆角实测) |
| r3 | 14 | 选中托底 |
| r4 | 24 | 两个面板容器(≈ 卡 r10 + 内边距 s4,同心规则) |
| — | 50% | 红绿灯 |

## 2. Token Reference

### 2.1 颜色(浅 / 深双值)

| Token | 浅色 | 深色 | 约束与理由 |
|---|---|---|---|
| `--accent` | `#007AFF` | `#0A84FF` | 系统聚焦环本色;深色提亮版沿用 Apple 自家换算,不另外发明 |
| 红绿灯 | `#FF5F57` `#FEBC2E` `#28C840` | 同左 | 系统规格,深浅不变 |
| `--txt-1` | `rgba(0,0,0,.85)` | `rgba(255,255,255,.87)` | 近似 labelColor;选中卡窗口标题(v1.1 焦点联动) |
| `--txt-2` | `rgba(0,0,0,.55)` | `rgba(255,255,255,.55)` | 未选卡窗口标题:辨识够用,存在感让位给截图 |
| `--plate` | `rgba(0,0,0,.06)` | `rgba(255,255,255,.10)` | 选中托底:刚好可见即停,深色系透明度约为浅色 1.7× |
| `--ko` | `rgba(250,250,252,.95)` | `rgba(22,23,27,.95)` | 聚焦环与截图间的剥离缝,取面板面色的近似色 |
| `--card-ring` | `rgba(0,0,0,.10)` | `rgba(255,255,255,.13)` | 未选卡 0.5px 纸边,防亮截图在亮玻璃上化开 |
| `--tb-tint` | `rgba(246,247,248,.62)` | `rgba(38,39,44,.62)` | 标题栏带:近实哑光。HTML 端**禁止** backdrop-filter(DOM 磨砂会叠在面板玻璃上,双重雾化,v1.7);原生 `.titlebar` 采样的是真实桌面,无此问题 |
| `--tb-line` | `rgba(0,0,0,.06)` | `rgba(255,255,255,.08)` | 带下发丝:真实窗口的物证,非装饰 |

### 2.2 液态玻璃(v1.10,单配方,两容器共用)

材质 = **透(tint)+ 磨砂(blur/saturate/brightness)+ 发丝轮廓(edge-ring)+ 失重影(2.3)+ 唯一的指针游光**。v1.10 裁决:rim 折光环、色散边、边缘透镜带三层 HTML 逼近**全部废除**(两轮评审连坐:亮框→内角;HTML 叠层逼近不了 NSGlassEffectView 的边缘折射,不做胜过做错——该职责归原生实现,见 §5)。

| 参数 | 浅色 | 深色 | 约束与理由 |
|---|---|---|---|
| tint | `rgba(252,252,254,.26)` | `rgba(28,32,42,.34)` | 灵魂是"看得见背后",不是"盖得住背后";上限 .45,超过即回磨砂板。v1.5 再降(浅 .33→.26 / 深 .42→.34):评审要求"大胆高透",面板形改由 rim + edge-ring + E 级收束,不靠 tint 盖 |
| ~~sheen~~ | 已删除(v1.8) | 已删除 | v1.0–v1.7 为 180° 顶光;与静态光泽叠层后成"磨砂盖",废除 |
| blur / saturate / brightness | 44px / 2.0 / 1.04 | 同左 / 1.02 | blur ≥36 才有水晶感;v1.4 40→44,把布景硬边再柔一档 |
| ~~折光环 `--rim`~~ | 已废除(v1.10) | 已废除 | 静态亮弧在边缘 = 边框,不是材质 |
| ~~色散边 `--fringe`~~ | 已废除(v1.10) | 已废除 | 同上;不变量 2 的色散豁免随层下线 |
| ~~静态光泽 `--spec-static`~~ | 已删除(v1.8) | 已删除 | v1.4 引入的"无人也活";v1.8 反向裁决:静置均匀大于静置发光,指针游光已是唯一的光 |
| 游动高光 | 径向 300×200,白,α `.24`,跟指针,离场 0.5s 归位 (50%, **−60%**,推出板外,v1.9) | α `.12` | 光是材质的一部分,不绑任何选中/hover 状态;`@property` 注册 `--sx/--sy` 使质心位置可过渡;停泊位在板外,静置顶缘不吃游光溢出 |
| 外轮廓 `--edge-ring` | 0.5px `rgba(0,0,0,.09)` | 0.5px `rgba(255,255,255,.09)` | rim 管"光",ring 管"形";v1.4 浅色 .07→.09:高透玻璃上,形线加半档才收得住 |
| ~~边缘透镜带 `.lens`~~ | 已废除(v1.10) | 已废除 | v1.9 撤光后残边仍在,评审拍板去掉:边缘折射归原生 `NSGlassEffectView`,HTML 不逼近 |

约束:原生施工以系统 Liquid Glass 材质为准(macOS 26+ `NSGlassEffectView`,边缘折射/透镜系统自带,**禁止**手刻);HTML 端只做 tint+blur+发丝+阴影+游光。高光半径为光学参数,**不计入** §1 的 4px 布局网格。

### 2.3 Elevation(阴影 = key 塑形层 + fill 锚定层)

层级即语义:**E3 浮窗 > E2 长条 > E1 卡片**。液态玻璃 = 失重:v1.3 起 α 整体下调、blur 加大。深色模式 α **必须** ×1.9(四舍五入;v1.2 及以前为 ×2.2)。

| 级 | 用途 | key(y / blur / α) | fill(y / blur / α) |
|---|---|---|---|
| E1 | 窗口卡静止 | 5 / 14 / .08 | 1 / 3 / .06 |
| E1-hover | 窗口卡悬停 | 7 / 18 / .10 | 1 / 4 / .07 |
| E1-sel | 窗口卡选中(静置) | 9 / 24 / .12 | 2 / 6 / .08 |
| E1-lift | 窗口卡选中(浮起,v1.1) | 13 / 32 / .15 | 3 / 8 / .10 |
| E2 | 图标长条 | 22 / 60 / .15 | 3 / 10 / .06 |
| E3 | 预览浮窗 | 30 / 80 / .19 | 5 / 14 / .08 |
| icon | 图标(贴合轮廓) | drop 4 / 10 / .15 | drop 1 / 2 / .10 |
| icon-sel | 选中图标 | drop 7 / 14 / .19 | drop 1.5 / 3 / .12 |

约束:**禁止**单层大阴影(糊成雾);**禁止**相邻层级交换(浮窗阴影弱于长条 = 层级反转)。

### 2.4 字阶

| 级 | 规格 | 用途 |
|---|---|---|
| t1 | 11px / 500 / 行高 1 / SF Pro Text | 窗口标题:**灯后 8px 左对齐**,右缘 12px,`min-width:0` 弹性尾省略;未选 `--txt-2`,选中 `--txt-1`(焦点窗标题更深的原生线索) |

产品面板内只有这一个字号。出现第二个字号之前,先回来改本文档。

### 2.5 动效

| 动作 | 时长 | 曲线 | 位移 |
|---|---|---|---|
| 面板弹出 | 120ms | `cubic-bezier(.2,.8,.2,1)` | scale .96→1 + 透明 |
| 浮窗出现 | 120ms | 同上 | translateY -5→0 + 透明 |
| 卡片入场 | 160ms/张,相邻延迟 18ms | 同上 | translateY -6→0 + scale .97→1 + 透明 |
| 浮窗换组 | 本体静止,仅卡片重排 | — | 横滑已传达"换组",本体再动是重复播报 |
| 浮窗滑移 | 180ms | `cubic-bezier(.25,.8,.3,1)` | 仅 `left` |
| 选中卡浮起(v1.1) | 220ms | `cubic-bezier(.34,1.35,.4,1)`(单次 settle) | translateY 0→-6px + scale 1→1.015,阴影→E1-lift;落选原路返回 |
| 状态切换 | 100ms | ease | 托底/环/阴影三属性**必须**同步 |
| 游动高光(v1.3) | 500ms | ease | 高光质心跟指针,离场归位(50%,-30%);注册 `@property` 使位置可过渡;不绑任何选中/hover 状态 |
| 图标入场错落(v1.4) | 220ms/个,首延迟 90ms,相邻 +14ms | `cubic-bezier(.34,1.35,.4,1)` | translateY 6→0 + scale .85→1 + 透明;与选中卡浮起共用 settle 曲线,一家手感 |
| Dock 磁吸放大(v1.4) | 指针驱动,无固定时长(100ms ease 平滑跟随) | — | f = max(0, 1 − d/1.8 格);`translateY(−6f px) scale(1 + .10f)`;上限放大 10%,影响半径 1.8 格;只写 transform,**不掺和选中态**(选中上浮 −1px 独立叠加),离场即清空 |
| 未选卡悬停上浮(v1.4) | 220ms | `cubic-bezier(.34,1.35,.4,1)` | translateY 0→−2px,阴影 E1→E1-hover;选中卡不响应(−6px 已在位) |
| 布景光斑漂移(v1.4,**演示专用**) | 46s alternate | ease-in-out | translate(3%, −2%) scale(1.06);给玻璃永远变化的光可折;非产品,不入动量审计 |

## 3. 组件解剖

### 3.1 Strip(图标长条,E2)

```
┌─ r24 ─────────────────────────────────┐
│  s4=16                                 │
│   ┌r14────┐  s2=8  ┌────┐              │
│s4 │ plate │········│cell│  …           │  cell = 80×80
│ 16│  icon │        │icon│              │  icon = 68×68(呼吸 6px)
│   └───────┘        └────┘              │
│  s4=16                                 │
└────────────────────────────────────────┘
```

- 选中托底:整格 `--plate`,r14,透明度 100ms ease;hover 仅到 45% 深度(主权在选中态)。
- 选中图标:`translateY(-1px)` + 阴影升 icon→icon-sel,与托底同帧。

### 3.2 Popover(预览浮窗,E3)

```
        ┌─ r24 ────────────── 中心正对选中图标 ─┐
        │ s4 ┌─ WindowCard ─┐ s4 ┌─ … ─┐ s4    │
        │    │              │    │     │       │
        └────┴──────────────┴────┴─────┴───────┘
                      s4=16(空气缝)
```

### 3.3 WindowCard(窗口卡,E1,r10)

```
┌ r10 ──────────────────────────┐
│ ● ● ● s2 <title 11/500 左对齐…>│ ← tbar 24px:近实哑光带,左右缘 s3,灯距 s2
├─ 0.5px --tb-line ─────────────┤
│                               │
│        截图 192px,等高          │ ← 宽随源图等比,禁止裁切定宽
│                               │
└───────────────────────────────┘
```

- 选中:`box-shadow` 双环 = 2px `--ko` + 2px `--accent`(v1.1 减环,蓝环与图之间留缝),同时阴影 E1→E1-lift、卡片浮起、标题 --txt-2→--txt-1(见动效表)。
- 红绿灯:10px 圆,带内垂直居中;内阴影三层(轮廓/受光/背光),禁止纯色平涂。
- 标题:左对齐、单行、超出 `…`;**禁止**居中(v1.1 评审:居中导致截断标题回流贴灯、各卡左缘节奏错乱)。双保险截断(v1.5):JS 侧超 **18 字符**先斩(`owner/repo:` 式前缀是辨识度最高的部分,15 会腰斩 `ejbills/DockDoor`,故取 18),CSS 宽度截断兜底中英混排。

## 4. 状态矩阵

| 组件 | default | hover | selected | 备注 |
|---|---|---|---|---|
| cell | — | plate 45% | plate 100% + icon 上浮 | hover 与 selected 叠加时按 selected |
| card | E1 + 发丝,标题 `--txt-2` | E1-hover | 双环 + 浮起 + E1-lift + 标题 `--txt-1` | 环/浮起/标题联动不参与 hover |
| popover | E3 | — | — | 无 hover 态(悬停即语义,见 brand-spec) |
| 无窗 App | — | 同左 | popover 整体隐去 | 禁止弹空壳 |
| reduced-motion | 全零 | 全零 | 瞬切 | 不变量 6 |

## 5. 原生实现映射(SwiftUI / AppKit)

| Token | 原生 |
|---|---|
| 玻璃 | macOS 26+ 用 `NSGlassEffectView`(液态玻璃);低版本回退 `NSVisualEffectView` `.popover`/`.active`;不手写底色 |
| 折光环/色散边/透镜带 | `NSGlassEffectView` 自带,**禁止**手刻;低版本回退放弃;HTML 原型不再逼近(v1.10) |
| Dock 磁吸放大 | 原型演示特性;原生默认**关闭**,是否挂设置开关另行评审——它不是选中语义的一部分 |
| E2/E3 | 两层 `NSShadow` 叠加或 `.shadow()` 两次;α 按深色 ×1.9 |
| 选中卡浮起 | `withAnimation(.timingCurve(0.34,1.35,0.4,1, duration: 0.22))`;全系统仅此一处允许欠阻尼,**禁止**换成 spring |
| icon 阴影 | 阴影打在 `NSImageView` 图片层(跟 alpha),不打在容器层 |
| 双环选中 | `StrokeBorder` 双层:内 2 `--ko`,外 2 `Color.accentColor` |
| tbar | 24pt 高 `.titlebar` material + 下缘 0.5pt `separatorColor` |
| 窗口标题 | `.system(size:11, weight:.medium)` + `.lineLimit(1).truncationMode(.tail)` + `.foregroundStyle(.secondary)` |
| 动效曲线 | `.timingCurve(0.2,0.8,0.2,1)` / `.timingCurve(0.25,0.8,0.3,1)`,禁止 spring |

## 6. 验收清单(每次合入前逐项过)

- [ ] 与 DockDoor 实物并排截图,玻璃/阴影/图标三样不糙一档
- [ ] 200% 放大:图标边缘无毛边、双环无断点、红绿灯圆度正
- [ ] 浅色/深色各过一遍状态矩阵 8 行
- [ ] 玻璃边缘零静态处理:无 rim/fringe/lens/sheen/静态光泽,唯余发丝轮廓与阴影
- [ ] 磁吸放大上限 10%,指针离场后 transform 全清空,选中托底不受扰
- [ ] 全文无 `#` 开头的第三种彩色(除 accent 与红绿灯)
- [ ] 全文无 spring / 振荡曲线,除选中卡浮起(220ms 单次 settle)外无 >200ms 动画
- [ ] 尺寸全部 4 的倍数(发丝线、红绿灯与玻璃光学参数除外)
- [ ] `prefers-reduced-motion` 下逐帧检查无位移

## 素材溯源

图标:本机 `/Applications` 各 App `.icns` 转出 384px PNG。窗口截图:IDEA / Xcode / Ghostty 为真实截屏;三张网页为 headless Chrome 对真实站点的渲染。**禁止**用 AI 生成图替换任何一帧。

## Changelog

### v1.1(评审:单窗卡"没高级感没呼吸感" + 选中要有浮起手感)
- 呼吸:长条/浮窗内边距 12→16、卡间距 12→16、面板圆角 20→24(同心规则,卡 r10 + s4)
- 选中环:2ko + 2.5accent → **2ko + 2accent**,蓝环与图之间留缝
- 动效:新增**选中卡浮起**——translateY(-6px) + scale(1.015) + 阴影 E1-lift,`cubic-bezier(.34,1.35,.4,1)` 220ms 单次落定;不变量 3 相应修订(禁振荡,不禁这一次欠阻尼)
- Elevation 阶梯新增 E1-lift 档

### v1.2(标题栏带文字重设计,评审:位置/省略/字级/颜色"都没设计")
- 标题弃全宽居中,改**灯后 8px 左对齐**、右缘 12px、`min-width:0` 弹性尾省略;修复截断标题回流贴灯与各卡左缘节奏错乱
- 标题颜色联动焦点:未选 `--txt-2` → 选中 `--txt-1`(macOS 活跃窗口标题更深的原生线索);选中态的三重信号 = 环 + 浮起 + 标题加深

### v1.3(液态玻璃,评审:"要更轻盈,上液态玻璃特效")
- 玻璃材质重写:tint 浅 .45→.30 / 深 .60→.42,blur 28→40,新增 brightness 微调;新增**折光环**(1px 渐变环,mask 双合成留边,光源左上方)与**游动高光**(跟指针,`@property` 注册使位置可过渡,离场 0.5s 归位)
- Elevation 失重化:全阶梯 α 下调约 25-30%、blur 加大;深色倍率 ×2.2→×1.9
- 玻璃解析新增约束:tint 上限 .45、rim 禁止整面描边、specular 不绑状态

### v1.4(液态玻璃全特效,评审:"深色浅色都要改,给我尽情上特效")
- 材质四要素→**五要素**:新增**色散边**(棱镜冷暖分离,顶蓝底粉,α≤.10——看得见颜色就算输)与**边缘透镜带**(10px 环带独立 `backdrop-filter` 二次折射,HTML 对 `NSGlassEffectView` 边缘透镜的逼近);rim 从 4 档线性升级为 7 档 conic,光源锁左上;新增静态左上光泽,玻璃无指针时也"活"着
- 参数重调:blur 40→44,saturate 1.9→2.0;游光 α .20/.10→.24/.12;浅色 tint .30→.33、`--edge-ring` .07→.09;E2/E3 浅色 key α .13→.15 / .16→.19(评审后回补:高透玻璃需要更实的锚)
- 动效新增:图标入场错落(220ms settle,90ms+14ms×i)、Dock 磁吸放大(10% 上限/1.8 格半径/transform-only,不染选中态)、未选卡悬停上浮 −2px;布景光斑 46s 漂移(演示专用,非产品)
- 不变量修订:第 2 条豁免色散分量;第 3 条明确 240ms 上限只管关键帧与状态过渡,材质光连续跟随与演示布景不受此限

### v1.5(评审:"标题超 15 字符省略?材质不够通透,大胆高透")
- 标题双保险截断:JS 超 **18 字符**截 `…`(不取 15——`ejbills/DockDoor` 本身就是 16 字符,15 会腰斩辨识度最高的前缀),CSS 宽度截断继续兜底中英混排
- tint 再降一档:浅 .33→.26、深 .42→.34;面板形由 rim / edge-ring / E 级收束,tint 只留"味",不盖"形"
- 原型角标加版本号(v4 标 "v4 · Liquid Glass",v3 存档标"已被 v4 取代"),治"开错旧文件"——同一评审连续两次基于 v3 截图

### v1.6(评审:"我要的是 app 的材质,不是把背景板弄通透")
- 布景换高饱和流动色带(浅色:蓝/杏/紫/薄荷/粉五色带 + 亮底;深色:蓝紫/紫/琥珀/青四色带 + 深底)——**液态玻璃只有压在有内容的背景上才透光**,平雾面背后任何 tint 都读成灰板;产品 tint 值不动(v1.5 的 .26/.34 在高饱布景下自然显形)
- 演示专用布景,非产品 token;浮动光斑同步换色

### v1.7(评审:高饱布景下标题栏带"为什么这么糊")
- 病因:tbar 的 `backdrop-filter: blur(18px)` 叠在浮窗玻璃上 = 磨砂叠磨砂的双重雾化,高饱布景被搅成脏雾;HTML 端 tbar **摘掉 backdrop-filter**,tint α .50→.62(浅/深同),改读近实哑光带
- 原生端不改:`NSVisualEffectView(.titlebar)` 采样的是面板背后的真实桌面,不存在叠加对象的错误,与 HTML 逼近路径在此分岔

### v1.8(评审:"深色下磨砂边框盖住了,要整体磨砂通透,不是边框")
- 灭"磨砂盖":删除 sheen 顶光与 `--spec-static` 静态光泽(五要素不变,光的构成收敛 = 指针游光一路 + 1px 发丝 rim);透镜带增亮/饱和 1.07/1.1→1.03/1.04,深色下不再"边框发光"
- 与 v1.4 的反向裁决备案:v1.4 要"无人也活",v1.8 要"静置均匀"——游光跟指针这一路的"活"已够,静置时再发光是噪音

### v1.9(评审:"还是一样,有明显的内角")
- 真凶到案:内角不是顶光,是**透镜带的亮度差**——brightness/saturate 微增量在平滑布景上把 10px 环带读成一圈内嵌亮框,内边界(半径 14 的圆角矩形)即"内角"。透镜带撤光,只留 `blur(2px)` 纯折射:环带与板心零亮度差,框感消失
- 游光停泊位 (50%, −30%) 推出板外至 (50%, −60%):静置时顶缘不再吃游光椭圆的下缘溢出
- v1.8 的"磨砂盖"与 v1.9 的"内角"同一课:**玻璃面内任何静态亮度差都是边框,不是材质**

### v1.10(评审:"这条边还是有,改不好就直接去掉")
- 边缘三层全废:rim 折光环、fringe 色散边、lens 透镜带一律下线;玻璃配方收敛为 tint + blur/saturate/brightness + `--edge-ring` 发丝轮廓 + E 级阴影 + 指针游光。原生 `NSGlassEffectView` 的边缘折射不再由 HTML 逼近
- 不变量 2 的色散豁免随 fringe 下线一并收回
