# Glance

macOS 窗口级 App 切换器:替换系统 App 中心激活,做到"鼠标在哪块屏,就只看哪块屏的窗;选中哪扇窗,就只拉起哪扇窗"。

## Language

### 语境与归属

**语境屏**:
面板触发那一瞬间,鼠标光标所在的显示屏。面板存活期间它是唯一世界,光标之后去哪都不再改变它(ADR-0001)。
_Avoid_: 当前屏、活跃屏、焦点屏

**按屏归属**(铁律):
每扇窗只归**一块**屏——它只出现在那块屏的面板里。**任何跨屏的候选集都不存在**。
多屏不是"更多选项",而是"各自独立的世界"(ADR-0008,用户裁决 2026-09-15)。
_Avoid_: 跨屏面板、聚合视图、跟随指针、全部窗口

**无窗应用**:
在跑、且**任何地方都没有窗**的 App——"任何地方"含其它 Space 与最小化的窗
(2026-09-19 收紧:判定面必须是全量窗清单。病例:一块屏全屏播放时,另一块屏普通
桌面上的 App 被误判成"无窗",两块屏的环里同时出现无点条目)。确认 = 激活。
_Avoid_: 没打开的 App、后台 App

**本屏窗**:
与语境屏几何交集占比最大的显示屏恰为语境屏、且交集占比 ≥20% 的窗口。
_Avoid_: 当前窗口、可见窗口

**不可见窗**:
被最小化、`⌘H` 隐藏、或未 ordered-in 的窗口。一律不进枚举、不被恢复——你都把它收走了,切过去请走 Dock。
_Avoid_: 隐藏窗、后台窗

**级联拉起**:
macOS 的原生疾病:激活一个 App 时它的全部窗口跨屏一并 raise。本产品存在就是为了不触发它。
_Avoid_: 窗口联动、拉窗

### 面板生命周期

**导航态**:
首次 `⌥+Tab` 击键到 `⌥` 释放(或 `Esc`)之间,面板所处的状态。期间一切按键与 hover 只移动选中,不产生任何副作用。(裸按 `⌥` 不进入导航态,只是待命)
_Avoid_: 预览态

**确认**:
释放 `⌥`,**或在窗口卡上点一下** → 聚焦当前选中的那一扇窗,面板关闭。唯一的"生效"动作。
(点窗口卡 = 确认,一直是现状;红绿灯按钮用 `Button` 与这个手势隔开,以免误触。)
_Avoid_: 提交、确定
_Avoid_: 提交、确定

**激活**:
让某个 App 成为前台(实现:`NSRunningApplication.activate`)。**代价是级联拉起**——
这个 App 的全部窗口跨屏一起 raise,正是本产品存在的理由。
_Avoid_: 拉起、启动、唤起(唤起已专指面板出现的那个动作)

**落焦**:
让**指定的那一扇**窗成为它所属 App 的 key window(实现:`makeKeyWindow(wid)` 私有 API
+ `kAXRaiseAction`)。**激活不保证落焦**——App 自己决定它内部哪扇窗拿键盘;
**本产品做的是落焦**,否则就退化成原生切换器。
`WindowFocuser.focus(window:)` 两步都异常时**降级**为纯激活(功能还在,但会级联拉起)。
_Avoid_: 聚焦、输入焦点

**放弃**:
`Esc` 或点击面板外任意处 → 面板关闭,不聚焦任何窗口。
_Avoid_: 取消

### 面板结构

**图标层**:
面板中 App 图标横排的区域,每个图标对应"本屏至少有一扇窗"的 App。
_Avoid_: 应用栏

**卡壳**:
窗口卡的**外形**部分——尺寸、圆角、描边、卡内留白。它决定一排卡有没有共同节奏。
_Avoid_: 卡片样式、外框

**卡面**:
窗口卡里"那一扇窗本身"的呈现:真截图,或统一底色 + 低透明度截图。
**卡面不统一,卡壳再整齐也会显得乱**(用户 2026-09-16:「窗口卡里面的内容不一样,有的 idea,
有的 chrome…在切换选择 app 的时候,视觉上可能会有点乱」)。
_Avoid_: 缩略图、预览图

**窗口卡**:
展开层里的**单个**本屏窗单位——一张缩略图,加上那个窗口的红绿灯与名字。
一次「确认」作用于一张窗口卡。
_Avoid_: 卡片、缩略图、预览图

**展开层**:
选中格正下方、内嵌于同一背板的**内容行** —— 托盘承载"选中格的东西":活跃格 → 窗口卡行。
停到图标即弹出(0ms,依赖预截/图标缓存)。
窗口卡画面 = 各扇窗自己的真实内容,天然**互不相同**("乱"的感知来源是属性,不是实时性)。
**非选中卡压色**(统一底色 + 低透明度截图)、选中的那张恢复原样 —— 统一交给底色,
"认得出"交给选中卡与画面结构(方案 D,2026-09-16 对照图拍板)。
_Avoid_: 预览区、浮条、实时预览

**入口槽**(已撤):
主环尾部的分隔缝格,曾是启动区的**门**(方案 E,2026-09-16 拍板)。死于 hover 误触:
指针扫向托盘的路径恰好经过它,扫过即切走整个展开层(2026-09-17 由 `↓`/`↑` 键取代);
双环模型 C(ADR-0013)之后连"门"的概念也不存在了 —— 未启动段是环的一段,不是门后的房间。
_Avoid_: 第二行、Dock 区

**未启动 App**:
Dock 常驻且**没在跑**的 App(2026-09-19 起收录面扩为:**Dock 常驻 ∪ 白名单 − 黑名单**,
仍在跑的不算)。与主环同属**一条逻辑环**的下一段(模型 C,ADR-0013,
2026-09-18 用户拍板,**推翻** 2026-09-16「不经 Tab 到达」的裁定——那条裁定保护的是已死的入口槽):
主环走到底 `Tab` 自然进入未启动段,`⇧Tab` 反向,`↓`/四指为直达快捷键,`↑` 返回主环。
呈现上**一次只显示一段**(宽度不叠加,长条尺寸不因未启动名单变宽)。
确认 = **启动并激活**,面板即关;启动成功后该 App 进主环。与「无窗应用」相对 —— 那个**在跑但没窗**,确认 = 激活。
最小化/隐藏的窗仍归「不可见窗」,不因此复活。
**白名单/黑名单**是这段收录面的两只调节阀,互斥(一个 App 只允许在其中一个名单里)。
_Avoid_: 未打开应用、已卸载应用、第二套环(它是同一条环的下一段)

**预截**:
面板出现的瞬间已完成的窗口截图。展开层只显示现成图,不现场截图。
_Avoid_: 实时预览、缩略图流

**破坏性键盘操作**:
导航态内 `Q`(退出选中 App)/`W`(关闭选中窗)/`M`(最小化选中窗),面板卡的红绿灯同义。**处决即散场**——动作一发本轮切换事务立刻死透,想继续切就重新触发一局;不留"它是退了还是只关了窗"的歧义状态。绿灯缩放不算处决,面板继续陪跑。
_Avoid_: 面板按钮、窗口管理,重枚举

### 排序证据

**MRU 证据**:
排序与高亮的近似数据源 = 前台 App 激活序列 + 每个 App 回答的 focusedWindow(ADR-0003)。不维护窗口级精确时间戳。
_Avoid_: 严格 MRU、窗口时间戳

### 效率的两个方向

产品的存在理由只有一个:**让切换这件事更省**。省在两个方向上,两者不冲突、可叠加。

**收敛**:
把候选集缩小到「本屏窗」——世界只有一块屏,选项从十几个降到两三个。
_Avoid_: 过滤、筛选、减少数量

**分流**:
在不移动手的前提下到达目标——手停在鼠标上也能完成选择,手停在键盘上也是。
_Avoid_: 单手操作、备用路径

### 手位与输入

**砍手(边界,ADR-0012)**:
指针场景的职责是**选择与确认**(hover 选中、点一下确认、双指滑动换组),
**破坏性动作不搬到鼠标上**(退出/关闭/最小化/全屏/隐藏一律只有键盘路径)。
_Avoid_: 拖拽删除、拖出去退出、手势处决

**指针场景**:
手停在指针设备上(触摸板或鼠标),另一只手在键盘(浏览网页、看文档、点界面)。
此时交互**不得要求把手移回键盘**。名字按"手在哪"命名,不写死具体设备。
_Avoid_: 鼠标场景、单手场景、日常场景

**键盘场景**:
双手都在键盘(写代码、TUI 交互)。此时交互应能用双手一次到位,不与鼠标场景互相妥协。
_Avoid_: 双手场景、开发场景

### 键盘交互

**双击修饰键**:
连续两次**干净地**按下同一个修饰键(如 ⌃),两次之间不许夹任何别的按键。
它是**普通键盘交互的一种形式**——不是鼠标手势,也不是热键的"补充入口"。
_Avoid_: 双击手势、双击快捷键

**原子操作**:
只做一件事的操作(如"把指针移到另一块屏")。产品的便利来自**暴露**原子操作,
而不是把它们合成一个"顺手帮你做完下一步"的动作(ADR-0007)。
_Avoid_: 融合操作、一键完成、组合动作

### 到达方式

同一件事可以有两种到达方式,它们**并存**且都保留 —— 不要因为"看着重复"就删掉一种(ADR-0009)。

**走**:
一格一格地移动到目标(`←/→` 之外的方向式移动、`Tab` 走组、`` ` `` 循环窗口)。
用于"还没想好去哪",或**不想用数字键**的时候。
_Avoid_: 逐格、浏览

**跳**:
直接到达指定目标(数字键直选、`←/→` 跳到两端)。
用于"已经知道要去哪"。
_Avoid_: 直达、快捷键(笼统)

### 二期功能(不进 MVP 验收)

**⌘+click 补焦**:
带 `⌘` 的左键点击落在非前台 App 的窗口时,只激活该窗而不级联拉起。
_Avoid_: 焦点修复

## 代码结构

**kernel**(纯逻辑,`Packages/GlanceCore`,`swift test` 可测)/ **plumbing**(AppKit 与事件层,留 App target)
的分家,模块图与依赖方向、命名约定见 `docs/architecture.md`;边界由 `Tools/check-architecture.swift` 校验。
