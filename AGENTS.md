# 与编码 agent 协作的约定(Glance)

本文件给在本仓库里干活的 agent 看。改动它请单独一个提交,并说明为什么。

## 提交:一次提交 = 一个可验证的成果,不是一次编辑

- 编辑期间**只编译、不提交**。工作区就是缓冲,草稿留在工作区里没有任何成本。
- 只在两种时机提交:
  1. 用户明确说"提交" —— 说了就提交,不要再问;
  2. 一件**可验收的事**完成且经用户实机验证(一个功能、一个 bug 的定型、一次设计定案)。
- **不要**"改一处 → 编译过 → 提交"这样串下去。那会把**修复的颗粒度**变成**提交的颗粒度**,
  历史变成一堆"修 XX"(2026-09-16 用户实评:「导致现在的提交记录超级多」)。
- 一个功能的多轮返工(例如 hover 换了三套做法)**压进同一个提交**,在信息里写清走过哪几条错路 ——
  那是这个仓库最有价值的部分,不要摊成三个"修 hover"。
- 提交信息用**病例体**:现象 / 证据(哪台机器、哪次测量、什么数字)/ 为什么这么改 / 否掉了什么。
- 历史的**形状**也算设计的一部分:宁可少而厚,不要多而薄。

## 工作方式

- **一次只动一处**:改 → 编译 → 看结果 → 再动下一处。不要写大段多点补丁(一处定位错就整段失败、零进展)。
- **构建通过 ≠ 改动落地**:编译只是最低门槛。改动是否真的生效,要靠日志/截图/实机。
- **脚本失败必须挡住构建与提交**:`PY_OK=$?; if [ $PY_OK -ne 0 ]; then …; fi`。
- 插入/删除代码时,锚点用"**我确知存在的那一行**",不要用"我以为的块尾"。
  在千行的 SwiftUI 视图里删"一整块"极不可靠 —— 优先用字符串替换达成目的(改颜色、改文案、改条件)。
- 改完之后复查一眼**自己刚写的东西**(重复挂载、悬空注释、被 return 掉的旧路径)。编译过了也可能错。

## 证据

- **"没有输出"不是证据**。`glog`/`trace` 都有开关:先确认渠道通电(同一份日志里有没有别的已知输出),
  再说"这件事没发生"。这一条在本仓库被踩过很多次(图标边缘、quarantine、弹窗过滤、trace 未开)。
- 诊断优先用**不带开关**的渠道:用户已经在看 Xcode 控制台 ⇒ 那一轮用 `NSLog` 比 `glog` 可信。
- 诊断日志**用完就删**,不要留在主路径上 —— 每秒上百次的 `NSLog` 会让界面明显发木。
- 平台层的怪症状(点击不响应、指针迟钝)先怀疑**窗口是否能成为 key**:
  AppKit 只把鼠标事件派发给 key window,而**无边框窗口的 `canBecomeKey` 默认为 false**。
- 用户说"还是有问题"时,先去**捞日志/看截图**,不要接着猜。

## 记录

- 设计决策进 `docs/adr/`;术语进 `CONTEXT.md`;病例进代码注释或 `docs/debugging.md`。
- 硬边界要写成 ADR,并写明"将来有人再提,先读它"。
- 面向用户的文案:不出现代码里的内部分类(例如"组");能用一行说完就不写两行。
